server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # Linha mágica para o roteamento do Angular funcionar
    try_files $uri $uri/ /index.html;
  }
}
